function Footer() {
  return (
    <footer className="site-footer">
      <p>Modern CSS Lab uses React for orchestration and vanilla CSS for layout, state, motion, and visual demos.</p>
      <p>Experimental features are labeled and ship with fallback visuals.</p>
    </footer>
  )
}

export default Footer
